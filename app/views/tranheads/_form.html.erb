<%= form_with(model: tranhead, local: true) do |form| %>
  <% if tranhead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tranhead.errors.count, "error") %> prohibited this tranhead from being saved:</h2>

      <ul>
      <% tranhead.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
  
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :tranhead_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
