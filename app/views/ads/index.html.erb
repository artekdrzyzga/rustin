
<%= form_tag ads_path, :method => 'get', :id => "ads_search" do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>

<% end %>
<div id="ads_list">
  <%= render :partial => "ads_list" %>
</div>
